<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ post.title }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/post.css">
    <link rel="stylesheet" href="/css/viewPost.css">
</head>

<body>
    <header>
        <nav class="nav-bar">
            <div class="items">
                <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-login-form/lotus.webp" alt="logo">
                <div class="nav-bar-items">
                    <ul>
                        <li><a href="/post">Home</a></li>
                        <li><a href="/post/create">Create</a></li>
                        <li><a href="/exit">Sair</a></li>
                    </ul>
                </div>
                <div class="search">
                    <input type="hidden" name="search" placeholder="Pesquisar" class="input-search">
                </div>
            </div>
        </nav>
    </header>

    <main>
        <div class="form-post">
            <div class="form-main">
                <p class="display-5" style="top: 5px; position:relative">{{ post.title }}</p>
                <div class="content">
                    <br>
                    <p style="font-size: 1.6em;">{{ post.content }}</p> <br>
                    <p><strong>{{ post.user.name }}</strong> {{ diffDates }}.</p>
                    <hr>
                </div>
                <form action="/comment/{{ post.id }}" method="post">
                    <label for="" style="font-size: 1.4em;">Comentar</label>
                    <input type="text" name="content" placeholder="Comentar" class="input-form-post">
                </form><br>
                <p class="display-6" style="text-align: left;">Comentários</p>
                <div class="comments">
                    {{#each comments }}
                        <div class="comment">
                            <strong>{{ this.comment.user.name }}</strong> • {{ this.diffHour }} <br>

                            {{ this.comment.content }}
                        </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous">
        </script>
</body>

</html>